<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Required fields for export are now configured directly in the questionnaire XLSX:
        set a question's Tag column (column J) to "REQ:Label" (e.g. "REQ:Scheme Name").
        All REQ-tagged questions must be answered before export is allowed.
        The first REQ field drives the survey card title; the first two drive the ZIP filename.
        No entries are needed here anymore.
    -->
</resources>
